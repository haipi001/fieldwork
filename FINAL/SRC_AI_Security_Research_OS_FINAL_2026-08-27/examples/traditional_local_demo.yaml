name: local-src-demo
mode: traditional_src
target:
  kind: url
  value: http://localhost:3000

scope:
  allow:
    - http://localhost:3000/**
  deny:
    - "*external*"
  active_testing: true
  rate_limit:
    requests_per_second: 3

report:
  platform: generic_src
  auto_submit: false

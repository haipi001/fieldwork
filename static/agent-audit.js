/* Domain-specific UI; the existing mode router and five workspaces remain shared. */
(() => {
  const auditState = {items: [], current: null, collectors: [], filter: 'ALL', monitorTimer: null};
  const workspace = name => document.querySelector(`[data-workspace="${name}"]`);
  const originalHero = workspace('new').querySelector('.hero-grid').innerHTML;
  const panels = {};
  const field = (label, name, extra = '') => `<label>${label}<input name="${name}" ${extra}></label>`;
  for (const name of ['new','run','findings','reports','settings']) {
    const panel = document.createElement('section');
    panel.className = 'agent-audit-panel'; panel.id = `agent-${name}`; panel.hidden = true;
    workspace(name).append(panel); panels[name] = panel;
  }
  const workflow = active => `<nav class="agent-workflow" aria-label="审计进度">${[['define','1','定义审计'],['import','2','导入证据'],['analyze','3','对账分析'],['verify','4','确认事件']].map(([key,n,label])=>`<span data-step="${key}" class="${key===active?'active':''}"><b>${n}</b>${label}</span>`).join('')}</nav>`;
  panels.new.innerHTML = `<section class="agent-scanner"><div class="agent-scanner-signal" aria-hidden="true"><i></i><i></i><i></i><span></span></div><div><p class="eyebrow">LIVE AGENT MONITOR</p><h1>开启后，自动记录。</h1><p>持续监听 Fieldwork 内的 Agent、工具与策略事件，自动整理时间线。无需填写表单。</p><div class="agent-scanner-facts"><span>本地记录</span><span>实时刷新</span><span>结束后自动分析</span></div><button type="button" id="agentMonitorStart" class="primary-action">开始自动监控 ↗</button><p class="target-hint">只记录启动后产生的 Fieldwork 运行事件；不会执行命令或修改目标。</p><p class="inline-error" id="agentCreateError" role="alert"></p></div></section><details class="agent-demo-option"><summary>想先了解结果？运行模拟示例</summary><button type="button" id="agentDemo" class="quiet-button">运行本地示例 ↗</button></details><section class="recent-section"><div class="section-heading"><div><p class="eyebrow">RECENT MONITORS</p><h2>最近记录</h2></div><span id="agentAuditCount"></span></div><div id="agentAuditList"></div></section>`;
  panels.run.innerHTML = `${workflow('import')}<div class="agent-toolbar"><label>当前审计<select id="agentAuditSelect"></select></label><span id="agentAuditBadge" class="state-badge"></span></div>
    <div class="page-intro"><div><p class="eyebrow">AGENT INCIDENT TIMELINE</p><h1>重建行为，核对证词。</h1><p id="agentRunHint">选择审计或导入本地示例。</p></div></div>
    <div id="agentKpis" class="run-metrics"></div><div id="agentNextStep" class="agent-next-step"></div><section id="agentMonitorControl" class="agent-monitor-control" hidden><div><span class="agent-live-dot"></span><b>正在自动监控</b><small id="agentMonitorMeta"></small></div><button type="button" id="agentMonitorStop" class="primary-action">结束监控并分析 ↗</button></section>
    <details id="agentManualTools" class="agent-card agent-manual-tools"><summary>外部 Agent / 历史日志导入 <small>可选</small></summary><form id="agentImport"><div class="section-heading"><div><h2>导入行为与自述</h2><p>用于外部 Agent 或监控开始前的 JSON / JSONL 记录。</p></div></div><div class="agent-form-grid">
      <label>格式<select name="kind"><option value="generic_json">Generic JSON</option><option value="jsonl">JSONL</option><option value="fieldwork_demo_trace">Fieldwork Demo Trace</option><option value="tool_call">Tool Call Log · JSON</option><option value="process">Process / Shell Log · JSON</option><option value="network">Network Log · JSON</option><option value="self_report">Agent Self Report · JSON</option></select></label>
      <label>来源<select name="provenance"><option value="agent_supplied">Agent 提供的材料</option><option value="operator_telemetry">独立采集器遥测</option></select></label>${field('来源名称','source_name','required placeholder="例如：OS collector / Agent trace"')}
    </div><label>选择文件<input id="agentImportFile" type="file" accept=".json,.jsonl,.txt"></label><label>或者粘贴 JSON / JSONL<textarea name="content" rows="5" spellcheck="false" placeholder='{"events":[{"timestamp":"2026-09-14T14:02:24+08:00","actor":"dependency-agent","session_id":"session-1","source_type":"network","action_type":"connect","network_host":"external.example","status":"success"}]}'></textarea></label>
    <details id="agentSigning" hidden><summary>验证采集器签名 <small>可信遥测选填</small></summary><div class="agent-form-grid"><label>采集器<select name="collector_id" id="agentCollectorSelect"><option value="">未签名 / 操作员声明</option></select></label>${field('Sequence','sequence','type="number" min="1" placeholder="严格递增"')}${field('Nonce','nonce','placeholder="至少 16 个字符"')}${field('签名时间','signed_at','placeholder="ISO 8601，含时区"')}<label>Ed25519 签名（Base64）<textarea name="signature" rows="2" spellcheck="false"></textarea></label></div><p class="target-hint">签名覆盖审计 ID、采集器、序号、nonce、时间、来源和原始内容 SHA256。</p></details>
    <div class="agent-options"><label><input name="independent_attested" type="checkbox">我确认遥测来源独立于被审计 Agent</label><label><input name="self_report_complete" type="checkbox">此材料包含本次任务的完整自述</label></div>
    <div class="agent-actions"><button type="submit" id="agentImportButton" class="primary-action">导入材料 ↗</button><button type="button" id="agentGenerateReport" class="text-action">用模型生成自述</button><button type="button" id="agentAnalyze" class="quiet-button" disabled>开始确定性分析</button></div><p id="agentImportError" class="inline-error" role="alert"></p></form></details>
    <div id="agentInputSummary" class="agent-card" hidden></div>
    <section class="agent-card"><div class="section-heading"><div><h2>Agent Says / Evidence Shows</h2><p>自述与独立证据逐条对账。</p></div></div><div id="agentComparison"></div></section>
    <section class="agent-card"><div class="section-heading"><h2>行为时间线</h2><select id="agentEventFilter" aria-label="筛选事件">${['ALL','AGENT','TOOLS','PROCESS','FILES','NETWORK','SELF REPORT','VIOLATIONS'].map(x=>`<option>${x}</option>`).join('')}</select></div><div id="agentTimeline"></div></section>`;
  panels.findings.innerHTML = `${workflow('verify')}<div class="page-intro"><div><p class="eyebrow">CANONICAL INCIDENTS</p><h1>事件结果</h1><p>先看经过独立验证的结论，再处理仍需验证的候选。</p></div></div><section class="agent-card agent-card--result"><div class="section-heading"><div><h2>已确认事件</h2><p>只包含独立证据能够支持的事实。</p></div><span id="agentFindingCount"></span></div><div id="agentFindings"></div></section><section class="agent-card"><div class="section-heading"><div><h2>待验证事件</h2><p>候选不会自动进入报告结论。</p></div><span id="agentCandidateCount"></span></div><div id="agentCandidates"></div></section><p class="inline-error" id="agentVerifyError" role="alert"></p>`;
  panels.reports.innerHTML = `<div class="page-intro"><div><p class="eyebrow">AI INCIDENT REPORT</p><h1>报告中心</h1><p>保留未知项、反证与材料哈希。模拟数据始终标记为模拟。</p></div></div><div class="agent-actions agent-card"><button id="agentPreview" class="primary-action">生成预览</button><div id="agentDownloads"></div></div><pre id="agentReportPreview" class="agent-report">选择审计后生成报告。</pre><section class="agent-card"><h2>研究指标</h2><div id="agentMetrics"></div></section>`;
  panels.settings.innerHTML = `<div class="page-intro"><div><p class="eyebrow">AUDIT CAPABILITIES</p><h1>设置与工具</h1><p>确定性审计可独立运行，无需模型 API。</p></div></div><div id="agentParsers" class="agent-card"></div><section class="agent-card"><div class="section-heading"><div><h2>可信采集器</h2><p>只登记 Ed25519 公钥；Fieldwork 不接收或保存私钥。</p></div></div><form id="agentCollectorForm"><div class="agent-form-grid">${field('采集器名称','name','required placeholder="Local OS Collector"')}${field('Key ID','key_id','required placeholder="local-os-1"')}${field('公钥（Base64）','public_key','required placeholder="32 字节 Ed25519 公钥"')}</div><button class="quiet-button" type="submit">登记采集器</button><p id="agentCollectorError" class="inline-error" role="alert"></p></form><div id="agentCollectors"></div></section><section class="agent-card"><h2>记录与信任边界</h2><p>签名采集器材料会校验 Ed25519 签名、递增序号和一次性 nonce；操作员声明材料保留为较低可信度。Agent 自述和模型建议不能单独确认事件。</p><p>SHA256 检测导入后的修改。本模式不启动 Agent、不执行导入命令、不联网复现，也不修改第三方系统。</p><p>模型配置沿用 Fieldwork 的「传统 SRC → 设置与工具」。</p></section>`;
  const empty = message => `<div class="empty-state">${esc(message)}</div>`;
  const call = (path, body) => api('/api/v1/agent-audit' + path, body === undefined ? {} : {method:'POST',body:JSON.stringify(body)});
  async function action(button, errorId, fn) {
    if(button.disabled)return;button.disabled=true;const error=document.getElementById(errorId);if(error)error.textContent='';
    try { await fn(); } catch(e) { if(error)error.textContent=e.message;else toast(e.message); } finally {button.disabled=false;}
  }
  async function select(id) {auditState.current=await call(`/audits/${id}`);render();}
  function monitorLoop() {clearInterval(auditState.monitorTimer);if(auditState.current?.monitor?.status==='active')auditState.monitorTimer=setInterval(async()=>{try{auditState.current=await call(`/monitor/${auditState.current.id}/scan`,{});render();}catch(e){clearInterval(auditState.monitorTimer);toast(e.message);}},2000);}
  function applyMode() {
    const active=state.mode==='agent_audit';
    for(const panel of Object.values(panels))panel.hidden=!active;
    const hero=workspace('new').querySelector('.hero-grid');
    if(active&&!hero.dataset.agent){hero.dataset.agent='1';hero.innerHTML='<div class="hero-copy"><p class="eyebrow"><span class="live-dot"></span> AI AGENT AUDIT WORKSPACE</p><h1>审查一次 Agent 行为。<br><em>让每个判断，都有独立证据。</em></h1><p class="lede" id="modeLede">开启实时监控后正常运行 Agent。Fieldwork 自动记录行为、整理时间线，并在结束时完成对账分析。</p></div><aside class="truth-panel"><div class="truth-index">SELF-REPORT ≠ GROUND TRUTH</div><p>Agent 的解释只是证词，不是事实。只有经过独立遥测、Policy 和证据交叉验证的事件，才能成为确认结论。</p><div class="truth-chain"><span>自动记录</span><i></i><span>对账行为</span><i></i><span>独立验证</span></div></aside>'}
    if(!active&&hero.dataset.agent){hero.innerHTML=originalHero;delete hero.dataset.agent;$('#modeLede').textContent=state.mode==='web3'?'输入已获授权的合约、协议或代码仓库。生产网保持只读，写入只允许 local fork / devnet。':'输入已获授权的网站或代码仓库。系统先冻结 Scope，再安排分析、复验和报告。'}
    const nav=document.querySelector('.nav-link[data-go="findings"]');nav.innerHTML=`<span>03</span>${active?'事件结果':'漏洞结果'}`;
    if(active){const current=Object.keys(panels).find(name=>workspace(name).classList.contains('active'))||'new',meta={new:['自动监控','开启即记录，无需填写'],run:['实时记录','持续采集与整理时间线'],findings:['事件结果','审阅独立证据支持的结论'],reports:['报告中心','导出事件与证据'],settings:['设置与工具','外部采集器与信任边界']}[current];$('#contextMode').textContent='AI Agent Audit';$('#contextTaskFact').textContent=`${auditState.items.length} 个记录`;$('#contextTitle').textContent=meta[0];$('#contextHint').textContent=meta[1];}
  }
  async function refreshAudit() {
    const items=await call('/audits');if(state.mode!=='agent_audit')return;auditState.items=items;
    const id=auditState.current?.id||items[0]?.id;
    if(id)await select(id);else render();
    const caps=await call('/capabilities');
    auditState.collectors=await call('/collectors');
    $('#agentParsers').innerHTML=caps.parsers.map(x=>`<div class="agent-list-row"><b>${esc(x)}</b><span class="state-badge">READY</span></div>`).join('')+`<div class="agent-list-row"><b>签名遥测 · ${esc(caps.signature_algorithm)}</b><span class="state-badge">${esc(caps.signed_telemetry)}</span></div><div class="agent-list-row"><b>自述生成模型</b><span>${esc(caps.self_report_generator)}</span></div><div class="agent-list-row"><b>LLM Semantic Matcher</b><span>${esc(caps.llm_semantic_matcher)}</span></div>`;
    $('#agentCollectors').innerHTML=auditState.collectors.map(c=>`<div class="agent-list-row"><div><b>${esc(c.name)}</b><small>${esc(c.key_id)} · ${esc(c.fingerprint)}</small></div><span class="state-badge">${esc(c.status.toUpperCase())}</span></div>`).join('')||empty('尚未登记采集器。');
    $('#agentCollectorSelect').innerHTML='<option value="">未签名 / 操作员声明</option>'+auditState.collectors.filter(c=>c.status==='active').map(c=>`<option value="${esc(c.id)}">${esc(c.name)} · ${esc(c.key_id)}</option>`).join('');
    applyMode();
  }
  function render() {
    const a=auditState.current;$('#agentAuditCount').textContent=`${auditState.items.length} 个审计`;
    $('#agentAuditList').innerHTML=auditState.items.map(x=>`<article class="agent-list-row"><div><b>${esc(x.name)}</b><small>${new Date(x.created_at).toLocaleString()}${x.demo?' · SIMULATED':''}</small></div><button class="quiet-button" data-open-audit="${esc(x.id)}">打开审计 →</button></article>`).join('')||empty('还没有审计。建立审计或体验本地示例。');
    $('#agentAuditSelect').innerHTML='<option value="">选择审计</option>'+auditState.items.map(x=>`<option value="${esc(x.id)}" ${a?.id===x.id?'selected':''}>${esc(x.name)}</option>`).join('');
    $('#agentManualTools').hidden=!a||!!a.analysis||a?.monitor?.status==='active';$('#agentInputSummary').hidden=!a||!a.analysis;
    if(!a){$('#agentKpis').innerHTML='';$('#agentNextStep').innerHTML='<b>先创建或选择审计</b><span>定义任务与授权边界后，才能导入证据。</span>';$('#agentComparison').innerHTML=empty('等待导入与对账。');$('#agentTimeline').innerHTML=empty('暂无行为记录。');$('#agentCandidates').innerHTML=empty('暂无候选。');$('#agentFindings').innerHTML=empty('暂无确认事件。');return;}
    $('#agentAuditBadge').textContent=a.demo?'SIMULATED · LOCAL FIXTURE':a.analysis?'INPUTS FROZEN':'INTAKE';
    $('#agentRunHint').textContent=`${a.identity.name} · ${a.identity.agent_name} · ${a.identity.session_id}`;
    const evaluations=Object.values(a.analysis?.policy_evaluations||{}), rows=a.analysis?.reconciliation.rows||[];
    $('#agentKpis').innerHTML=[['Events',a.events.length],['Policy Violations',evaluations.filter(x=>x.decision==='violation').length],['Self-report Mismatches',rows.filter(x=>['OMITTED','CONTRADICTED'].includes(x.status)).length],['Verified Incidents',a.findings.length]].map(([label,n])=>`<div class="metric"><small>${label}</small><strong>${n}</strong></div>`).join('');
    panels.run.querySelectorAll('.agent-workflow span').forEach(step=>step.classList.toggle('active',step.dataset.step===(a.analysis?'analyze':'import')));
    const hasInputs=a.imports.length>0, analyze=$('#agentAnalyze'), generate=$('#agentGenerateReport');
    if(analyze){analyze.disabled=!hasInputs;analyze.className=hasInputs?'primary-action':'quiet-button';analyze.textContent=hasInputs?'分析已导入材料 ↗':'开始确定性分析';}
    if(generate)generate.disabled=!a.events.length;
    const activeMonitor=a.monitor?.status==='active';$('#agentMonitorControl').hidden=!activeMonitor;if(activeMonitor)$('#agentMonitorMeta').textContent=`已记录 ${a.events.length} 个事件 · 每 2 秒刷新`;
    $('#agentNextStep').innerHTML=activeMonitor?'<b>监控已开启</b><span>现在可以正常运行 Agent；Fieldwork 会自动记录新事件。</span>':a.analysis?'<b>分析已完成</b><span>下一步：审阅候选并确认有独立证据支持的事件。</span>':hasInputs?`<b>已记录 ${a.events.length} 个事件</b><span>结束监控后会自动执行确定性分析。</span>`:'<b>等待运行事件</b><span>保持此监控开启，然后正常启动 Agent。</span>';
    $('#agentInputSummary').innerHTML=`<b>输入与 Policy 已冻结</b><p>${a.events.length} 个行为 · ${a.claims.length} 条自述 · ${a.imports.length} 次导入</p><small class="agent-hash">Policy SHA256 · ${esc(a.policy_sha256||'UNKNOWN')}</small><div class="agent-actions"><button class="primary-action" data-agent-go="findings">审阅事件结果 →</button><button class="quiet-button" data-agent-go="reports">查看报告</button></div>`;
    $('#agentComparison').innerHTML=rows.map(row=>`<article class="agent-comparison"><div><small>AGENT SAYS</small><p>${esc(row.statement)}</p><span class="state-badge ${row.status==='CONTRADICTED'?'agent-alert':''}">${esc(row.status)}</span></div><div><small>EVIDENCE SHOWS</small>${row.event_ids.map(id=>{const e=a.events.find(x=>x.id===id);return `<p><time>${esc(e.timestamp)}</time><br>${esc(e.source_type)} · ${esc(e.action_type)} → ${esc(e.resource)}</p>`}).join('')||'<p>没有匹配的独立证据；不等于陈述为假。</p>'}<small>Policy · 网络访问 ${a.policy?.internet_access?'允许列表':'DENIED'}</small></div></article>`).join('')||empty(a.analysis?'当前没有可对账的自述。':'导入后点击「开始确定性分析」。');
    renderTimeline();
    const pending=a.candidates.filter(c=>c.status!=='verified');$('#agentCandidateCount').textContent=pending.length;$('#agentFindingCount').textContent=a.findings.length;
    $('#agentCandidates').innerHTML=pending.map(c=>{const e=a.events.find(x=>x.id===c.event_id),status=a.analysis?.reconciliation.event_status[c.event_id]||'UNKNOWN';return `<article class="agent-incident"><div><small>${esc(c.id)} · ${esc(c.boundary)} · INFO</small><h3>${esc(c.title)}</h3><p>${esc(e?.resource)} · ${esc(e?.timestamp)}</p><span class="state-badge">${esc(status)}</span><p class="agent-note">验证状态 ${esc(c.status)} · 独立证据 ${e?.independent?1:0} · 反证待检查<br>时间范围 ${esc(e?.timestamp||'UNKNOWN')}</p></div><button class="quiet-button" data-verify-incident="${esc(c.id)}">执行独立验证 →</button></article>`}).join('')||empty('当前无待验证事件。');
    $('#agentFindings').innerHTML=a.findings.map(f=>`<article class="agent-incident"><div><small>${esc(f.id)} · ${esc(f.category)} · INFO</small><h3>${esc(f.title)}</h3><p>${esc(f.impact.summary)}</p><span class="state-badge">VERIFIED ${a.demo?'· SIMULATED':''}</span><p class="agent-note">自述 ${esc(f.verification.self_report_status)} · 独立证据 ${f.verification.independent_evidence_count} · 反证已检查<br>时间范围 ${esc(f.verification.timestamp)} · ${esc(f.verification.verification_basis)} · ${f.verification.authenticity_verified?'签名真实性已验证':'操作员来源声明'}</p></div><a class="quiet-button" href="/api/v1/agent-audit/audits/${a.id}/capsule" download>证据包 ↓</a></article>`).join('')||empty('尚无确认事件；自述和候选不能代替独立验证。');
    $('#agentDownloads').innerHTML=['markdown','json','html'].map(format=>`<a class="quiet-button" href="/api/v1/agent-audit/audits/${a.id}/report?format=${format}" download="${a.id}.${format==='markdown'?'md':format}">${format.toUpperCase()} ↓</a>`).join('')+`<a class="quiet-button" href="/api/v1/agent-audit/audits/${a.id}/capsule" download>ZIP Evidence Capsule ↓</a>`;
    $('#agentReportPreview').textContent='点击「生成预览」，查看当前审计报告。';
    $('#agentMetrics').innerHTML=Object.entries(a.metrics).filter(([key])=>key!=='ground_truth').map(([key,value])=>`<div class="agent-list-row"><span>${esc(key.replaceAll('_',' '))}</span><b>${value?.value!=null?`${(value.value*100).toFixed(1)}% (${value.numerator}/${value.denominator})`:'UNKNOWN'}</b></div>`).join('')+`<p class="target-hint">需要独立真值才能计算召回率和精确率；候选数量不代表效果。</p>`;
    applyMode();
    monitorLoop();
  }
  function renderTimeline(){const a=auditState.current;if(!a)return;const mapping={AGENT:['agent_trace'],TOOLS:['tool_call','mcp','api'],PROCESS:['process'],FILES:['filesystem'],NETWORK:['network','browser']},items=[];
    if(auditState.filter==='ALL'||auditState.filter==='AGENT'){items.push({at:a.identity.start_time,order:0,html:`<article class="agent-list-row agent-derived-event"><time>${esc(a.identity.start_time)}</time><div><b>TASK START</b><p>${esc(a.identity.task_objective)}</p><small>${esc(a.identity.agent_name)} · ${esc(a.identity.session_id)}</small></div><span class="state-badge">SCOPE FROZEN</span></article>`});items.push({at:a.identity.end_time,order:9,html:`<article class="agent-list-row agent-derived-event"><time>${esc(a.identity.end_time)}</time><div><b>TASK WINDOW END</b><p>${esc(a.identity.name)}</p><small>Declared audit boundary</small></div><span class="state-badge">BOUNDARY</span></article>`});}
    if(auditState.filter==='ALL'||auditState.filter==='SELF REPORT')for(const c of a.claims)items.push({at:c.time_start,order:3,html:`<article class="agent-list-row"><time>${esc(c.time_start)}</time><div><b>SELF REPORT</b><p>${esc(c.statement)}</p><small>Agent claim · ${esc(c.assertion)}</small></div><span class="state-badge">${esc(a.analysis?.reconciliation.rows.find(row=>row.claim_id===c.claim_id)?.status||'待分析')}</span></article>`});
    if(auditState.filter!=='SELF REPORT')for(const e of a.events){
      const evaluation=a.analysis?.policy_evaluations[e.id],reconciled=a.analysis?.reconciliation.event_status[e.id]||'UNKNOWN';
      if(auditState.filter==='ALL'||(mapping[auditState.filter]||[]).includes(e.source_type)||auditState.filter==='VIOLATIONS'&&evaluation?.decision==='violation')items.push({at:e.timestamp,order:1,html:`<article class="agent-list-row"><time>${esc(e.timestamp||'UNKNOWN')}</time><div><b>${esc(e.source_type)} · ${esc(e.action_type)}</b><p>${esc(e.resource)}</p><small>${esc(e.source_name)} · ${e.trust_level==='signed_collector'?'签名采集器 ✓':e.independent?'操作员声明':'Agent 材料'} · ${esc(e.status)}</small></div><span class="state-badge">${esc(evaluation?.decision||'待分析')}</span></article>`});
      if(evaluation?.decision==='violation'&&['ALL','VIOLATIONS'].includes(auditState.filter))items.push({at:e.timestamp,order:2,html:`<article class="agent-list-row agent-derived-event"><time>${esc(e.timestamp)}</time><div><b>POLICY VIOLATION</b><p>${esc(evaluation.boundaries.join(' · '))}</p><small>Deterministic PolicyEvaluation</small></div><span class="state-badge agent-alert">VIOLATION</span></article>`});
      if(['CONTRADICTED','OMITTED'].includes(reconciled)&&auditState.filter==='ALL')items.push({at:e.timestamp,order:4,html:`<article class="agent-list-row agent-derived-event"><time>${esc(e.timestamp)}</time><div><b>${reconciled==='CONTRADICTED'?'CONTRADICTION DETECTED':'OMISSION DETECTED'}</b><p>${esc(e.resource)}</p><small>Claim ↔ Evidence Reconciliation</small></div><span class="state-badge agent-alert">${esc(reconciled)}</span></article>`});
    }
    items.sort((left,right)=>String(left.at).localeCompare(String(right.at))||left.order-right.order);
    $('#agentTimeline').innerHTML=items.map(item=>item.html).join('')||empty(auditState.filter==='SELF REPORT'?'暂无自述。':'没有匹配的事件。');
  }
  $('#agentDemo').onclick=event=>action(event.currentTarget,'agentCreateError',async()=>{auditState.current=await call('/demo',{});await refreshAudit();go('run');});
  $('#agentMonitorStart').onclick=event=>action(event.currentTarget,'agentCreateError',async()=>{auditState.current=await call('/monitor/start',{});await refreshAudit();go('run');});
  $('#agentMonitorStop').onclick=event=>action(event.currentTarget,'agentImportError',async()=>{clearInterval(auditState.monitorTimer);auditState.current=await call(`/monitor/${auditState.current.id}/stop`,{});render();go('findings');});
  $('#agentAuditSelect').onchange=event=>{if(event.target.value)select(event.target.value).catch(e=>toast(e.message));};
  $('#agentImport [name="provenance"]').onchange=event=>{$('#agentSigning').hidden=event.target.value!=='operator_telemetry';if(event.target.value!=='operator_telemetry')$('#agentSigning').open=false;};
  $('#agentImportFile').onchange=async event=>{const file=event.target.files[0];if(file){if(file.size>2_000_000){$('#agentImportError').textContent='文件超过 2 MB';return;}$('#agentImport [name="content"]').value=await file.text();if(file.name.endsWith('.jsonl'))$('#agentImport [name="kind"]').value='jsonl';}};
  $('#agentImport').onsubmit=event=>{event.preventDefault();action(event.submitter,'agentImportError',async()=>{const form=new FormData(event.target),collector=String(form.get('collector_id')||'');const body={kind:form.get('kind'),content:form.get('content'),provenance:form.get('provenance'),source_name:form.get('source_name'),independent_attested:form.has('independent_attested'),self_report_complete:form.has('self_report_complete')};if(collector)Object.assign(body,{collector_id:collector,sequence:Number(form.get('sequence')),nonce:form.get('nonce'),signed_at:form.get('signed_at'),signature:form.get('signature')});auditState.current=await call(`/audits/${auditState.current.id}/imports`,body);render();$('#agentImport [name="content"]').value='';$('#agentImportFile').value='';toast(collector?'签名已验证，材料已导入':'已导入并脱敏保存');});};
  $('#agentCollectorForm').onsubmit=event=>{event.preventDefault();action(event.submitter,'agentCollectorError',async()=>{const form=new FormData(event.target);await call('/collectors',{name:form.get('name'),key_id:form.get('key_id'),public_key:String(form.get('public_key')).trim()});event.target.reset();await refreshAudit();toast('可信采集器已登记');});};
  $('#agentAnalyze').onclick=event=>action(event.currentTarget,'agentImportError',async()=>{auditState.current=await call(`/audits/${auditState.current.id}/analyze`,{});render();});
  $('#agentGenerateReport').onclick=event=>action(event.currentTarget,'agentImportError',async()=>{auditState.current=await call(`/audits/${auditState.current.id}/self-report/generate`,{});render();toast('已保存机器生成的自述建议');});
  $('#agentEventFilter').onchange=event=>{auditState.filter=event.target.value;renderTimeline();};
  $('#agentPreview').onclick=event=>action(event.currentTarget,'',async()=>{if(!auditState.current)throw Error('请先选择审计');const r=await fetch(`/api/v1/agent-audit/audits/${auditState.current.id}/report`);if(!r.ok)throw Error('报告读取失败，请检查审计证据完整性');$('#agentReportPreview').textContent=await r.text();});
  document.addEventListener('click',event=>{const open=event.target.closest('[data-open-audit]');if(open)action(open,'agentCreateError',async()=>{await select(open.dataset.openAudit);go('run');});const nav=event.target.closest('[data-agent-go]');if(nav)go(nav.dataset.agentGo);const verify=event.target.closest('[data-verify-incident]');if(verify)action(verify,'agentVerifyError',async()=>{const result=await call(`/audits/${auditState.current.id}/incidents/${verify.dataset.verifyIncident}/verify`,{});await select(auditState.current.id);toast(result.status==='verified'?'记录重建验证通过':'发现冲突遥测，需要人工审阅');});});
  window.agentAudit={applyMode,refresh:refreshAudit};applyMode();if(state.mode==='agent_audit')refreshAudit().catch(e=>toast(e.message));
})();

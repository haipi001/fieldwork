# 产品补全执行状态

目标：补全产品，最后同步到本地 Fieldwork App。范围以 PRODUCT_REVIEW_2026-09-07.md 和 FINAL 原始设计为基准，不以本轮已实现内容重定义完成。

## A 可信底座

- [x] 统一结构化导出脱敏、避免 JSON 文件重名、ZIP 逐文件 SHA-256 和独立无解压校验。
- [x] 修复 read_only_completed 误报；观察置信度不等于 tested；终态未复验不标完成。
- [x] 服务端 VerificationReceipt：HTTP/ptai/Forge 属性复测均由真实执行路径内部签发，绑定 Candidate、Run、Scope、Artifact 与有效期；Web3 另绑定不可变项目规则快照，伪造或篡改均拒绝。
- [x] 候选安全语义门槛、每项假设精确证据引用与普通观察分层。
- [x] HTTP/ptai/Forge 实际 Artifact、归属/哈希校验、环境/步骤/预期材料与版本化 replay contract 已接入；HTTP/ptai 支持离线记录断言，Forge 支持第二进程隔离源码重放。目录外 Oracle 明确不支持。

## B 候选复验产品闭环

- [x] 候选详情、证据查看、分诊/方法/前置条件；普通观察与重复项进入可追溯排除历史。
- [x] HTTP / Web3 受控验证计划、后台执行、进度/失败/取消：统一 Verification Job 查询/取消合同；Web3 两轮独立 seed 复测完成后才生成正式收据。
- [x] 反证、影响材料与报告字段补全，不绕过机器收据：Verified Finding 可补充报告摘要、复现前置条件、受影响用户/资产、成立条件、影响 Evidence、反证摘要与 Evidence、修复建议和平台字段；服务端校验证据归属与正反极性，编辑内容与收据绑定的机器证明分层保存。

## C 原始设计补全

- [x] 专用 Target/规则导入，规则变更与授权有效性：OpenAPI、Postman Collection、HAR 支持本机安全预览、来源选择、脱敏表面导入和确认前草稿；源码 ZIP 具备路径穿越、符号链接、大小、压缩比与文件类型门禁，导入为可追溯本地仓库；CIDR 支持规范化、重叠合并、规模限制和逐请求 IP 归属门禁。Web3 项目规则支持来源、全文哈希、版本、到期、资产和影响类别校验，并完成版本差异、重新授权和最新授权版本强制使用。
- [x] 项目环境资产关系：根目标、环境、Observation 资产及来源关系可视化；旧项目可从历史观察补图，未授权子域与未观察边界单独标记。
- [ ] 跨 Run 根因去重、生命周期、修复复测：同根因合并、出现历史、修复声明、新 Run 定向候选和再次复现自动重开已完成；HTTP 对象读取可通过两轮身份有效、所有者基线、非所有者/未登录明确拒绝的负向收据确认 `verified_fixed`。Forge/ptai 的负向修复确认仍待补。
- [x] 可移植证据包与第二实例验证：ZIP 逐文件完整性、记录断言与 Forge 源码/命令合同已实现，Forge 正例已由独立 Python 进程在临时目录重放。

## D 深度与效率

- [x] 有明确支持边界的 Oracle 目录与正负样本：设置页公开 HTTP 对象读取、Forge 属性和 ptai 胶囊方法的适用范围、前置条件、正反例、升级权限和可移植级别；目录外类型保持人工复核。
- [x] 业务模板与 Campaign→Candidate→Finding 联通：内置对象所有权、流程顺序、账本一致性、租户权益四类模板，模板实例仍经过冻结 Scope 和声明式工作流校验；Campaign 的强异常信号自动建立 Candidate 并保留轮次/流程/假设关联，只有经过现有独立复验收据门槛才可进入 Canonical Finding。
- [ ] Web3 跨合约/经济影响证据与项目资格。
- [ ] 真实模型用量、成本核算、增量/缓存与动态规划。

## E 发布与本地 App

- [ ] 干净安装/恢复/升级与授权项目验收。
- [ ] 更新版本、资源缓存与本地 App；启动后视觉/交互验收。
- [ ] 按全部要求逐项完成审计；仅全部证据充分时标记目标完成。

当前：进行中。首次目标执行轮已修改导出与状态逻辑，新增篡改、敏感标记、重复文件名及状态回归测试。未同步发布半成品到桌面 App，未发起外部测试。

验证：106 项全量 pytest 通过；58 文件完整性与 64 项静态合同通过；Python 编译、JS 语法与 diff 检查通过。新增状态测试最初没有终止运行，补充显式停止步骤后通过。服务端验证收据尚未修复，不应宣称可信验证已完成。

第二轮进展：新增 verification_receipts.py（内部签发，无公开签发 API），现有 verification_attempts 持久化收据。收据绑定候选内容、Run、Scope、完整 proof 字段及实际 Artifact SHA-256，有效期 24h；人工声明不能签发。旧 Web3 自报成功测试改为拒绝测试，真实 HTTP 测试补报告/附件正路径和篡改导出拒绝。旧无收据结论仍可查看，但不能导出机器证明包，需复验。接下来补候选语义与 UI，Web3 的完整机器收据仍在范围内。

第二轮验证：114 项全量 pytest 通过；Python 编译及 git diff --check 通过。未更新版本/重启本地 App，统一发布仍待全部要求收口。

第三轮进展：普通服务/路由/页面发现不再仅因多来源而生成漏洞候选；Native Agent 要求安全边界、预期/实际、影响、验证方法及精确观察引用，拒绝公开目录和无绑定假设。候选详情抽屉显示来源证据、历史分诊提示和下一步，可进入 HTTP 工作台；完整验证计划和后台执行仍待补。历史候选保留，不自动清理。

第三轮验证：117 项全量测试通过，另新增候选详情只读与证据归属聚焦测试 1 项通过；Python/JS 语法、diff 检查通过。临时数据副本预览过详情抽屉，预览服务已停止。本地 App 未发布更新，目标仍进行中。

第四轮进展：HTTP 对象读取 Oracle 升级为身份与所有权语义断言，要求两个不同且有效的身份、同一候选对象、所有者字段绑定、未登录负对照拒绝与两轮稳定结果；旧式“响应与基线相同”只能进入人工复核。候选详情加入验证计划预览、后台执行、持久化进度、失败/重启中断状态和协作取消，任务表不保存凭据。新增候选分诊，将安全假设、缺少证据、普通观察与重复项分开；V0 普通信息不再展示不适用的 HTTP 机器验证入口。

第四轮验证：121 项全量 pytest 通过；58 文件包与 64 项静态合同通过；Python/JS 语法及 diff 检查通过。临时数据库副本完成桌面宽屏视觉与无控制台错误检查，未向实际候选目标发送复验请求，预览服务已停止。Web3 正式验证、C–E 范围与最终本地 App 发布仍未完成。

第五轮进展：Web3 Property Candidate 已形成正式的“两轮 Forge 定向复测 → 稳定反例 → 项目规则导入/选择 → 服务端收据 → Canonical Finding”链路。规则快照要求已审阅、未过期、目标资产在范围内、影响类别有严重度映射，并保留 known issue、历史审计与 PoC 政策来源；规则内容变化会使既有收据失效。属性复测改为统一后台任务，UI 展示轮次、阶段、进度、失败和协作取消。另修复 Traditional/Web3 快速切换时旧工作域报告异步回写到当前页面的数据串域问题。

第五轮验证：124 项全量 pytest 通过；新增 Web3 后台完成与轮间取消、规则版本/过期/错 Scope、规则篡改收据拒绝测试。58 文件包、64 项静态合同、Python/JS 语法和 diff 检查通过。使用实际数据库的隔离副本复查 Web3 空状态和快速工作域切换，当前工作域不会再显示另一工作域的运行报告；未向任何实际目标执行复验，预览服务已停止。本地 App 仍未发布，C–E 的其余范围继续进行。

第六轮进展：项目资产关系图已把根目标、环境、观察资产和证据来源联通，并为旧数据动态补图；严格按照 Scope 区分根目标内与根目标外观察。CanonicalFinding 新增跨 Run 根因指纹、出现次数、修复声明、定向复测计划和再次复现自动重开，且禁止复用最近证据 Run 冒充复测。Proof Bundle 新增版本化 replay contract；HTTP 与 ptai 可在无网络下复核记录断言，Forge 会携带有限源码树和两轮命令，可由第二个 Python 进程在新临时目录主动重现。设置页新增验证方法目录，明确支持边界、正反样本与人工复核降级。

第六轮验证：129 项全量与聚焦 pytest 通过（全量基线 128，新增目录聚焦 1）；58 文件包、64 项静态合同、Python/JS 语法和 diff 检查通过。使用实际数据库回放确认 49 个资产、48 条关系在 UI 正常归组；验证方法目录完成浏览器可访问性树与响应检查。所有预览服务均已停止，未向实际目标发送新请求。本地 App 仍未发布；负向修复确认、其他 Target/规则导入、业务模板、Web3 经济证据、真实成本/缓存和发布验收继续进行。

第七轮进展：修复声明后选择新 Run 会自动复制原始根因形成定向候选并直达候选详情。HTTP 对象读取修复确认新增 `fix-verification-receipt/1`：只有两个身份仍有效、基线所有者绑定成立、非所有者和未登录访问连续两轮均被明确拒绝且结果稳定时，生命周期才进入 `verified_fixed`；其他结果保持人工复核。正向复测若通过会合并同一 CanonicalFinding、自动重开并清理计划占位候选。新建分析支持本机导入 OpenAPI、Postman Collection 与 HAR；预览仅保留方法、无查询参数 URL 和来源哈希，多来源只把选定根来源放入 Scope，Header、Cookie、Body、变量和其他来源默认不导入，创建后仍必须确认 Scope。

第七轮验证：133 项全量 pytest 通过；58 文件包、64 项静态合同、Python/JavaScript 语法和 diff 检查通过。目标包导入对话框已完成浏览器可访问性检查，预览服务已停止。未向实际目标发送请求，本地 App 仍等待全部范围收口后统一发布。

第八轮进展：Web3 项目规则新增独立授权账本。首次导入直接记录审阅授权；后续版本自动比较规则正文、Scope 资产、影响类别与级别、已知问题来源、审计来源、PoC 政策、有效期及来源地址。发生变化时，新版本进入 `pending_reauthorization`，候选页展示结构化差异并暂停正式结果生成；人工填写授权说明后才恢复。服务端同时拒绝未授权的新版本和已被新版本替代的旧规则，历史收据仍按原不可变快照哈希复核。既有已审阅规则在数据库升级时安全回填授权状态。

第八轮验证：134 项全量 pytest 通过；新增规则变化阻断、明确确认、快照不可变、旧版本拒绝和无变化版本自动确认测试。58 文件包、64 项静态合同、Python/JavaScript 语法与 diff 检查通过。浏览器验收确认版本状态、差异清单、授权表单和折叠导入入口均可访问且视觉层级正常；临时 QA 页面已删除，未向实际目标发送请求。本地 App 仍等待剩余 C–E 范围完成后统一发布。

第九轮进展：统一“导入目标资料”入口现覆盖接口描述、源码 ZIP 与 CIDR。源码 ZIP 以 base64 在本机传输，20 MB 压缩包、100 MB 解压总量、2,000 文件及 5 MB 单文件为上限，拒绝路径穿越、符号链接、隐藏文件、依赖/构建目录与异常压缩比，仅落盘受支持源码和构建配置，并保留逐文件哈希及来源哈希。CIDR 导入要求显式前缀，合并重叠网段、限制单网段地址数，并让 HTTP 请求按目标 IP 是否属于冻结网段通过门禁；CIDR 执行仅规划 `httpx` 存活发现，不把域名枚举或浏览器 Agent 错用于网段。所有导入结果仍为 draft，必须确认 Scope 才可执行。

第九轮验证：137 项全量 pytest 通过；新增安全源码解压、非源码忽略、路径穿越拒绝、CIDR 合并及网段内外请求门禁测试。58 文件包、64 项静态合同、Python/JavaScript 语法与 diff 检查通过。实际新建页浏览器验收确认统一入口、资料类型、文件与项目名称字段清楚可用，未导入实际目标或发起网络请求。本地 App 尚未发布。

第十轮进展：长期 Campaign 增加对象所有权、流程顺序、账本集合一致性、租户与权益边界四类引导模板。模板只生成声明式只读流程，URL、步骤依赖与机器不变量继续走既有 Scope/工作流校验。Campaign 执行中的强异常信号会从对应 Observation 建立 Candidate，并通过独立关联表保留 Campaign、轮次、流程、长期假设与候选关系；普通失败、无 Observation 或非可复验类别不会自动生成候选。Campaign 页面新增证据晋升路径、候选列表和复验入口，明确 Candidate 仍需独立重放、负对照与影响确认，不能直接成为 Finding。

第十轮验证：138 项全量 pytest 通过；新增四类模板目录、模板应用、越界 URL 拒绝、未知模板拒绝，以及“机器不变量失败生成 Candidate 但 Canonical Finding 保持为零”的证据门禁测试。58 文件包、64 项静态合同、Python/JavaScript 语法与 diff 检查通过。隔离临时数据库的实际浏览器验收确认模板卡、动态参数表单、Scope 校验反馈、生成后的流程摘要和候选晋升区均正常；未执行任何目标请求，临时服务已关闭。本地 App 尚未发布。

第十一轮进展：Verified Finding 新增“影响与报告资料”编辑器。报告上下文包括摘要、前置条件、可复核影响、受影响用户或资产、成立条件与限制、影响 Evidence、反证/负对照说明及 Evidence、修复建议和平台自定义字段。资料端点只接受当前 Finding 已绑定 Evidence，反证必须为 counter 极性，影响依据不能引用 counter；所有人工编辑保存在 `verification.editorial` 与 `impact.editorial`，不会改写收据绑定的复验步骤、原始影响声明或 Artifact。通用报告模型和 Markdown 现输出前置条件、影响依据、反证与修复建议；报告中心单独显示必填完整度和建议补充项。

第十一轮验证：140 项全量 pytest 通过；完整 HTTP 对象授权 Oracle 回归验证错误反证极性被拒绝、合法报告资料保存、收据 ID 保持不变、报告包含正向影响依据与反证 Evidence。Proof export 聚焦 10 项通过；FINAL 58 文件、64 项静态合同、Python/JavaScript 语法与 diff 检查通过。正式数据库当前没有 Verified Finding，未写入 QA 假数据；发布前仍需在隔离数据中完成该抽屉的最终视觉验收。本地 App 尚未发布。


2026-09-11 Web3 0.32.0：源码绑定的编译器 AST 调用关系、修饰器/重载解析、优先假设及分析页已接入。三类本地经济漏洞的正反样本可一键双 seed 执行、核对经济断言并下载复现源码。145 全量测试通过。实际协议经济影响、跨合约运行时绑定仍未完成，夹具结果不晋升为真实项目 Finding。用户要求本轮更新桌面应用。

桌面更新状态：0.32.0 已安装到 /Applications/Fieldwork.app，签名验证通过；旧应用和数据库已备份。新进程启动正在等待 macOS 文稿文件夹访问确认（TCC 日志已证实）；系统权限窗口禁止自动化操作，桌面 UI 验收尚未完成。实战证据 ZIP 在独立临时目录双轮重放通过。

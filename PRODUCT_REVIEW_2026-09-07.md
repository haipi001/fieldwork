# Fieldwork 总体产品复核与补全路线

日期：2026-09-07
基准：FINAL/SRC_AI_Security_Research_OS_FINAL_2026-08-27；当前工作区实现及用户提供的 Findings 截图。
性质：代码与产品链路复核，不是外部目标渗透测试或完整安全审计。本轮未执行任何目标扫描、候选复验、归档或数据清理。

## 结论

Fieldwork 已有真实工具执行、授权门禁、研究流程、长期 Campaign、Web3 本地分析与桌面应用，远超静态原型。但尚未完成最初北极星“一个授权目标 → 自动研究 → 独立证明 → 完整交付”。当前主要短板是候选质量、复验闭环、证据可信度和产品可操作性，而非工具数量。

此前 IMPLEMENTED 状态及 99 项本地测试通过仅能证明相应代码路径和测试断言。不能据此宣称候选质量、机器验证、实际影响、平台资格或跨机器重现全部验收通过。旧 Proof Backlog 中工具缺失等历史状态也不能直接沿用。

## 一、用户标记的「候选 / 待验证」如何使用

当前 Traditional 页面只支持编辑标题与判断说明、保存、归档。保存不执行复验，也不会升级成 Verified。归档只隐藏记录，不代表已证伪。截图中的“产品页透露品牌”“编号反映目录规模”通常只是公开信息观察，单凭这些不能作为漏洞候选；需要明确违反的安全边界和可证明的影响。

当前已有手工 HTTP 工作台：在分析过程选择 Traditional Run，查看/构造受控请求、修改重放、响应差异及加入候选。但这与结果页中的候选尚未形成统一复验操作流。

Traditional HTTP 三组请求两轮复验与 ptai capsule replay 有后端接口，未接入结果页向导。Web3 只有特定 web3_property_violation 候选有两轮属性复测入口，稳定失败是 reproduced，仍不能替代部署对齐、反证、影响和规则资格。

用户理应能在候选页完成：

查看证据 → 分诊（安全假设 / 普通观察 / 重复 / 无效）→ 选择验证方法 → 补测试身份/对象/预期行为 → 预览受控计划 → 执行复验 → 查看两轮与反证 → 补影响 → 生成报告。

不支持自动复验的类型必须显示“需人工复核”和缺失材料，不得添加无依据的“一键通过”。

## 二、已经确认的优先问题

### P0-01 验证接口信任调用方结论

位置：final_core.py:1399–1464；tests/test_final.py:1792。

verify_candidate 主要根据请求提供的 reproduced、attempts、counterevidence_checked、文本说明和 Oracle 名称决定 Verified；没有强制查询与当前候选绑定的、服务端执行产生的复验收据，也不验证传入 PoC Artifact IDs 必须存在并属于该运行。impact.demonstrated 直接设为 true。

现有测试把 Oracle 名称从 demo 改为 http-state-replay-v1，未执行对应 HTTP Oracle，也可升级并生成可导出报告。这是当前测试预期本身过宽，不是通过测试就能排除的问题。

要求：建立服务端生成的 VerificationReceipt；强制核对候选、Scope、环境版本、两轮证据、负对照、Artifact 哈希与归属。人工判断单独标识 human-reviewed，不能冒充机器验证。直接调用接口填写成功布尔值应被拒绝。

验收：不存在/跨 Run/过期/被篡改收据全部拒绝；仅修改 Oracle 名称不能升级；有效真实复验仍能通过。

### P0-02 候选生成缺少安全语义门槛

位置：final_core.py:1374；native_agent.py:171。

correlate_observations 按同 subject 的观察数量 >= 2 建候选；没有要求被违反的安全属性或攻击者影响。Native Agent 输出只检查少量字段便写入 Candidate，并将最近三条页面观察关联为依据，缺少每条假设与具体证据的严格对应。

要求：将普通资产信息、发现线索、漏洞假设明确分层；Candidate 至少包含资产、前置条件、预期/实际差异、安全边界、影响假设、证据定位、适用 Oracle。多个工具看到同一页面只能提高资产信息可信度。

验收：产品目录、公开品牌、正常跳转、普通 200 响应不入候选；明确越权或不变量破坏可入候选；模型产生无证据假设被降级。

### P0-03 结果页面缺少复验工作台

位置：static/final.js:24、65；traditional_runtime.py:325、409；web3_analysis.py:690。

要求：候选详情抽屉提供来源证据、验证等级、适用方法、缺失前置条件、计划预览、执行进度、反证与影响材料。复验使用后台任务，可停止、可恢复且复用 Scope/预算门禁。先实现支持明确语义的 HTTP 身份边界与现有 Web3 属性复测，再扩展类型。

验收：用户不打开终端完成一个本地真实候选从查看到报告；不支持的 Oracle 有明确下一步；不允许界面勾选“已验证”绕过收据。

### P0-04 覆盖与阶段状态有误导

位置：final_core.py:1258、1275；traditional_tools.py:598。

- native-agent 成功记录 read_only_completed，但 get_run_details 只认可 completed/passed/compiled，因此误显示失败。
- Run 终态时将 verification/impact 等阶段统一显示 completed，不能证明实际做过复验或影响评估。
- record_observation 中以置信度 >= .7 标记 tested，观察可信度不是安全测试完成度。

要求：区分执行状态、测试覆盖、验证等级、研究结论。维护统一结构化状态枚举，显示“工具完成、待复验”“部分失败”“不适用”等真实语义。Coverage 绑定测试目录条目与执行收据，不由观察置信度决定。

验收：只读研究完成不误报失败；没有 Oracle 收据时验证阶段不显示已验证完成；失败/未测不计入安全测试通过。

### P0-05 导出边界与完整证据包不足

位置：reporting.py:151；final_core.py:1659。

export_bundle 当前 ZIP 包含 report、结构化 model、完整度、evidence_manifest、package_manifest；未携带完整实际 PoC、请求响应、复现脚本和环境资料。proof_capsule 的 portable=True 与 JSON SHA-256 不等于在干净实例成功重现。

临时目录的虚构标记探针确认：相同内容在 report.md 被脱敏，在 report.json 原样保留；因此导出层没有统一递归脱敏保障。本轮没有扫描真实密钥，也未证实既有导出包包含真实秘密。

要求：结构化递归脱敏并覆盖 JSON/Markdown/HTML/SARIF/附件；引用解析必须核对归属与哈希；导出实际证据文件、环境版本、预期结果、复现入口与逐文件 manifest；区分“可检查证据”与“可执行重现”，不默认声明 portable。

验收：各格式虚构敏感标记全部被移除；缺失或篡改 Artifact 阻断 Ready；第二个干净实例可校验并重现支持的本地证明。

## 三、与原始设计的逐项差距

| 能力 | 当前判断 | 缺口与验收方向 |
|---|---|---|
| 五工作区、本机工具执行 | 已有可用主路径 | 继续以用户端闭环验收，不能只看按钮存在 |
| 广泛 Target 输入 | 部分 | URL/仓库/地址基础识别已有；OpenAPI、源码压缩包、CIDR、Program URL 不能都算完整接入。Web3 含斜线文本容易归为仓库；需专用解析器与可解释拒绝 |
| 项目规则与 Scope | 有基础门禁 | 自动读取规则、结构化条款、规则差异、过期与重授权流程不完整；快照存储不能替代规则理解 |
| 项目资产模型 | 部分 | 当前主要是一项目一 Target；缺项目→环境→多资产→端点/源码/部署的可操作关系视图 |
| 自动研究规划 | 部分 | 主流程为工具链加有限只读导航；缺基于 Coverage Gap 的任务自适应选择、假设优先级与稳定止损 |
| 候选质量与证据对应 | 明确不足 | 见 P0-02；增加检索相关性、具体证据定位、反例和分诊 |
| Oracle/自动复验 | 有接口，未闭环 | 当前 HTTP Oracle 是 baseline/attack 相同且 negative_control 不同的固定签名判据；不足以统一覆盖 XSS、SSRF、注入、逻辑与代码类问题，且响应相同本身不证明越权 |
| 身份与复杂业务逻辑 | 已有较强本地基础 | Keychain、矩阵、多步/分支/依赖/逆序补偿/并发/不变量/调度已有；仍需会话有效性、角色对象绑定、引导配置及真实授权流程验收 |
| Web3 分析 | 部分较深入 | 编译、语义模型、属性复测与部署对齐已有；协议经济不变量、跨合约资产流、统一 trace/storage/balance 证据及可实现影响仍未形成通用产品链路 |
| Web3 项目接入与资格 | 部分 | ProgramSnapshot 版本存储不等于 Immunefi 页面规则导入；known issue/audit/影响资格布尔字段不等于依据驱动的核查 |
| 影响与严重性 | 部分 | 主要接收文字或字段；缺可复核的影响推导、严重度依据、受影响对象和前置条件证明 |
| 跨运行生命周期 | 部分 | 现有保存/归档、Graveyard、Campaign hypothesis 状态；缺统一根因去重、重复关联、修复版本与定向回归。旧 v1 指纹函数不能证明 v2 完成闭环 |
| 平台报告 | 基础可用 | 配置驱动完整度与多格式导出已有；缺项目自定义字段编辑、缺失材料反向导航、真实附件与平台 overlay 端到端验收 |
| 成本与模型路由 | 部分 | 有预算扣减，但 Native Agent 每轮按固定 250000 微美元预留；未按真实 token usage 对账。router/plan 主要静态工具映射，不是完整成本优化器 |
| 缓存与增量 | 未见完整主路径 | 原始设计要求 commit/scope/tool/config/input 共同键控与失效；项目变化扫描、影响传播及去重缓存需独立验收 |
| 发布与迁移 | 本地基础已有 | 备份/回滚/启动检查已有；项目级迁入迁出、独立重现、诊断包预览、干净机器安装恢复还需证据 |

说明：以上“缺口”主要指主产品链路及其验收，不宣称仓库中绝无相关局部代码。历史工具缺失记录不作为当前安装状态依据。

## 四、总体设计本身需要补足的内容

### 1. 将“全面能力”写成边界清楚的能力矩阵

按环境、输入、漏洞类别、身份要求、Oracle、证据、自动化等级定义每一格的支持状态：自动证明 / 可辅助验证 / 仅发现线索 / 尚不支持。不能以工具安装数或 Candidate 数替代。

当前范围继续聚焦 Traditional Web/API/Repo 与 EVM。Mobile、Cloud、其他链是后续 Runtime，不应全部混入当前发布承诺。

### 2. 完整的状态与权限模型

分开：Run 执行状态、Test 覆盖状态、Candidate 验证等级、Finding 生命周期、Report 完整度。为人工决策与机器事实分别记录来源。明确哪些状态由机器转换、哪些需要人，以及失败后的下一步。

### 3. 可执行的验证协议，而不是一个通用 verify 表单

每个 Oracle 插件定义支持类别、输入结构、前置条件、成功断言、负对照、稳定性要求、环境限制、证据格式和升级权限。已有 HTTP 差异只能声明其适用范围。机器证明不能由 LLM 自评或人工填布尔值替代。

### 4. 两条长期闭环合并

Campaign Hypothesis 和 Candidate/Finding 需要有显式关系：研究假设→测试计划→反证/复验→Finding→修复→回归。保留历史失败和重新打开条件，避免两套列表互相脱节。

### 5. 证据来源与数据边界

将网页/仓库/工具输出当作不可信数据；设计提示注入负例、证据绑定验证、模型上下文脱敏、外部模型发送范围与可见记录。导出和日志使用统一结构化脱敏器，不能依赖各模块自行处理。

### 6. 数据和桌面应用长期维护

明确单机本地服务边界、Origin/会话防护、数据库迁移、更新失败回退、备份保留、磁盘不足、端口占用和恢复演练。此项是设计补充，未在本轮执行完整 localhost 攻击面审计。

### 7. 质量度量与独立基准

核心指标：人工审阅后有效候选比例、每个高价值发现的验证成本、稳定复现率、错误 Verified 数、支持范围内的漏检率、可补测覆盖缺口、复现包成功率和用户无终端完成率。

现有 logic-v4 本地夹具有价值，但还需盲测样例、正常业务负对照、非安全公开信息、动态响应、权限拒绝、模型误导和真实授权项目。按漏洞类别分别评分，避免综合分掩盖空白。

### 8. 工程扩展方式

逐步拆分 final_core 的授权、执行、候选、验证、证据、报告、Campaign 模块；统一状态合同与版本化事件。保持现有栈，采用增量重构，不以重写拖延关键闭环。

## 五、推荐实施顺序与退出条件

| 阶段 | 交付物 | 退出条件 |
|---|---|---|
| A 可信底座修复 | 服务端 VerificationReceipt、统一导出脱敏、真实状态映射、候选安全语义分层 | 伪造验证拒绝；普通观察不混入候选；所有导出标记脱敏；状态不矛盾 |
| B 无终端复验闭环 | 候选详情/证据查看、验证方法、受控计划、后台复验、反证、影响与报告入口 | 一个真实本地正例与一个正常负例均可从 UI 完整处理；未知类型不冒充支持 |
| C 原始产品能力补全 | 专用目标/规则导入、资产树、真实 Proof Bundle、跨 Run 去重与修复回归 | 干净实例可检验证据并重现；同根因多次出现能合并并保留历史 |
| D 深度研究与运行效率 | Oracle 目录扩展、业务模板、Web3 经济/跨合约证明、真实用量计费、增量调度 | 各支持类别都有独立正负样本和预算/中断恢复验收 |
| E 发布验收 | 干净机器安装、恢复与升级演练、授权 Traditional/Web3 项目、非技术用户试用 | 无终端主流程完成，失败可恢复，跨机器包可验证，数据边界通过 |

不建议现在优先添加更多扫描器、云协作或自动平台提交。最先完成 A+B，才符合“简单外表、严格内部”的原始原则。

## 六、本轮验证记录

- 前一轮全量 pytest：99 passed，仅作为已有回归基线。
- 本轮聚焦测试：候选→verify→report、关联候选、Native Agent 候选保守状态，共 3 passed；其中 verify 测试明确暴露当前信任调用方的宽松合同。
- 临时目录导出探针：仅虚构 AUDIT_SENTINEL 标记，无真实凭据；Markdown 脱敏 true，JSON 脱敏 false；实际附件目录不存在。
- 原始设计文档、产品差距记录、实现、测试及用户截图交叉核对。
- 未修改产品逻辑；后续实施应将上述问题转成修复与拒绝路径回归测试。

# Web3 挖掘能力补强（2026-09-11）

用户已确认继续完善应用实战能力，并要求更新桌面应用。

## 已实现

源码入口风险分析沿同一合约内可唯一识别的内部调用传播读写、外部调用和低级调用。循环调用通过访问集合终止；不可达函数不污染入口。入口记录保留 reachable_internal_functions、unresolved_internal_calls 和 source_heuristic 标识。重载名称不猜测，抽象声明不再误用下一函数的实现体。

这些结果是研究假设，不是漏洞证明。源码函数记录保留直接效果，入口记录展示可达效果。现有 discovery API、入口风险排序和研究假设入库自动使用增强结果。

## 下一阶段与验收标准

1. 编译器 AST 与跨合约解析：用声明 ID 区分重载、继承、modifier、库、this/super 调用；动态目标显式 unresolved。验收同名合约、继承覆盖、重载、代理与正反路径夹具。
2. 协议经济不变量：先做 Vault/ERC-4626 存取款和份额核算，再做借贷抵押率、清算和预言机。每项绑定输入域、单位、舍入方向、外部资金流、允许误差；有漏洞与修复版本在同一条件下比较。
3. 本地 Fork 影响证据：固定链和区块、部署代码哈希、起始资本、角色权限、资产前后余额、债务、费用与交易 trace；跨资产分别计量，没有价格依据不换算利润。记录外部注资，避免把注资误当收益。
4. 证明闭环：两轮独立复现 + 修复或反事实负对照 + 项目影响类别映射；余额声明必须来自执行器收集并绑定证据哈希，不能由用户填写直接升级。
5. Web3 独立基准：对会计、权限、回调、预言机、签名、代理各设正负样本；分别统计候选召回、误报和可复现比例，不以工具安装数代表挖掘能力。

尚未实现通用跨合约资产流、经济损失证明或上述全部路线。现有工作区的其他修改保留；本轮不代表桌面应用已更新。

## 0.32.0 本轮补强

- 新增编译器 AST 分析：以完整 build-info 的声明 ID 解析重载、内部调用、修饰器、继承和带类型的外部接口调用。每个编译单元独立；所有编译输入逐字节匹配当前源码后才采纳。虚调用、低级调用与未知目标明确保留，外部静态类型不等于部署目标绑定。
- 编译器关联的状态写入与交互生成优先研究假设，并在分析页呈现；不会直接变成 Verified Finding。
- Web3 首页新增本地实战校验：三类漏洞（账本漂移、越权提款、份额舍入）各有漏洞/修复正反对照，另外有两个固定经济测量用例。两轮独立 seed，每个 fuzz 测试每轮 64 次；总计 16 个测试结果、768 次 fuzz 输入。
- 本地份额模型核对攻击者本金、收益与受害者损失，数量使用十进制字符串与 wei，未扣 gas。该模型不是完整 ERC-4626 实现。
- 校验下载 ZIP 含源码、哈希、实际测试结果与独立重放命令；仅内置夹具，不能作为真实协议经济影响证明。
- 真实 Forge 编译与实战正反测试通过；全量 145 passed，58 文件与 64 静态合同通过。

后续仍需对具体授权协议适配经济不变量、运行时代理/外部目标、storage alias 和 Fork trace；不能据此宣称任意协议已可自动完整审计。

工具参数依据：[Foundry forge inspect/build-info 文档](https://getfoundry.sh/forge/reference/forge-inspect/)。

桌面更新状态：0.32.0 已安装到 /Applications/Fieldwork.app，签名验证通过；旧应用和数据库已备份。新进程启动正在等待 macOS 文稿文件夹访问确认（TCC 日志已证实）；系统权限窗口禁止自动化操作，桌面 UI 验收尚未完成。实战证据 ZIP 在独立临时目录双轮重放通过。
